name: Deploy to Hostinger

on:
  push:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install JS dependencies
        run: npm install

      - name: Build frontend assets
        run: npm run build

      - name: Deploy via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          source: "app,bootstrap,config,database,public,resources,routes,storage,artisan,composer.json,composer.lock,.env.example"
          target: "~/domains/candys-commande.skopee.fr/public_html/"
          overwrite: true

      - name: Run post-deploy commands via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          script: |
            set -e
            cd "$HOME/domains/candys-commande.skopee.fr/public_html"
            APP_PATH="$(pwd)"
            echo "APP_PATH=$APP_PATH"

            if [ ! -f .env ]; then
              cp .env.example .env
              php artisan key:generate --force
            else
              # Fix APP_NAME if unquoted (contains space without quotes)
              sed -i 's/^APP_NAME=\([^"]\+[[:space:]]\+[^"]\+\)$/APP_NAME="\1"/' .env
            fi

            composer install --no-dev --optimize-autoloader --no-interaction

            php artisan migrate --force
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            rm -rf public/storage
            ln -snf "$APP_PATH/storage/app/public" "$APP_PATH/public/storage"
            echo "Deploy complete"